type: object

required:
  - name
  - email
  - password

properties:
  name:
    type: string
    example: Umut Saatci

  email:
    type: string
    format: email
    example: umut@example.com

  password:
    type: string
    format: password
    minLength: 8
    example: Password123!
